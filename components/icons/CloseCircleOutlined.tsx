import { Icon } from '.';
import { IconRestProps } from './Icon';

const CloseCircleOutlined = (props: IconRestProps) => (
  <Icon name='close-circle' {...props} />
);

export default CloseCircleOutlined;
