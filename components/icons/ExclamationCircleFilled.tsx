import { Icon } from '.';
import { IconRestProps } from './Icon';

const ExclamationCircleFilled = (props: IconRestProps) => (
  <Icon name='warning-circle-fill' {...props} />
);

export default ExclamationCircleFilled;
