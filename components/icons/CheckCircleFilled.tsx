import { Icon } from '.';
import { IconRestProps } from './Icon';

const CheckCircleFilled = (props: IconRestProps) => (
  <Icon name='check-circle-fill' {...props} />
);

export default CheckCircleFilled;
