import { Icon } from '.';
import { IconRestProps } from './Icon';

const DoubleLeftOutlined = (props: IconRestProps) => (
  <Icon name='doubleleft' {...props} />
);

export default DoubleLeftOutlined;
