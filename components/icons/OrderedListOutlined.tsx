import { Icon } from '.';
import { IconRestProps } from './Icon';

const OrderedListOutlined = (props: IconRestProps) => (
  <Icon name='orderedlist' {...props} />
);

export default OrderedListOutlined;
