import { Icon } from '.';
import { IconRestProps } from './Icon';

const InfoCircleOutlined = (props: IconRestProps) => (
  <Icon name='info-circle' {...props} />
);

export default InfoCircleOutlined;
