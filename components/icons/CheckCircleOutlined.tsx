import { Icon } from '.';
import { IconRestProps } from './Icon';

const CheckCircleOutlined = (props: IconRestProps) => (
  <Icon name='check-circle' {...props} />
);

export default CheckCircleOutlined;
