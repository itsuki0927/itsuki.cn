import { Icon } from '.';
import { IconRestProps } from './Icon';

const DoubleRightOutlined = (props: IconRestProps) => (
  <Icon name='doubleright' {...props} />
);

export default DoubleRightOutlined;
