import { Icon } from '.';
import { IconRestProps } from './Icon';

const CloseCircleFilled = (props: IconRestProps) => (
  <Icon name='close-circle-fill' {...props} />
);

export default CloseCircleFilled;
