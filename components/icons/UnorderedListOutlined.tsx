import { Icon } from '.';
import { IconRestProps } from './Icon';

const UnorderedListOutlined = (props: IconRestProps) => (
  <Icon name='unorderedlist' {...props} />
);

export default UnorderedListOutlined;
